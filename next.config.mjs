/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.ctfassets.net', 'i.sbxcars.com'], // List of allowed domains
    },
  };
  
  export default nextConfig;
  